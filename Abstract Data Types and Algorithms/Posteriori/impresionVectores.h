#pragma once

// Imprime un vector de enteros por pantalla, con los elementos seguidos pero separados por un espacio. Si el tamaño
// es mayor o igual que 10, se imprime por pantalla un mensaje de advertencia en vez de los elementos
// Parámetros:
// - vector: puntero al inicio del vector
// - n: numero de componentes del vector
// Precondición: vector != NULL
void escribir(int *vector, int n);
